local module = {}

local part=path

instance->check->destroy

function module.giveDamage(Player)
    Player.humanoidrootPart.
end

function module.create(Player)
    local newCreature = part.clone()
    newCreature.parent= game.Player.LocalPlayer
end

fuction module.deleteObject(delete)
    local delete.destroy()
end